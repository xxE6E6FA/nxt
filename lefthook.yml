pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.go"
      run: golangci-lint run ./...
    test-short:
      glob: "*.go"
      run: go test -short ./...

post-commit:
  commands:
    install:
      glob: "*.go"
      run: make install
